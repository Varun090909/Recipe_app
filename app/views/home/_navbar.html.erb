<!DOCTYPE html>
<html>
  <head>
    <title>Recipe App</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %> 
    <%= javascript_importmap_tags %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  </head>
    <body>
    <nav class="navbar navbar-expand-lg navbar-light bg-light" id="navbar">
  <div class="container-fluid">
        <a class="navbar-brand" href="/home">Home</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
          <%if user_signed_in? %><br>
          <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/users/sign_out">Log-out</a>
        </li>
          <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/users/edit">Edit Profile </a>
        </li>

        <li class="nav-item">
        <%= link_to "My Recipes", "/recipes", class: "nav-link active" %>
        </li>

          <li class="nav-item">
        <%= link_to "All Recipes", "/home", class: "nav-link active" %>
        </li>
 


        <li class="nav-item">
        <%= link_to "Create Recipe", new_recipe_path(@recipe), class: "nav-link active" %>
        </li>
        <%else%>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/users/sign_in">Login</a>
        </li>
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/users/sign_up">Sign-up</a>
        </li>
        <%end%> 
      </ul>
         <%if user_signed_in? %><br>
      <%# <form class="d-flex" method = "GET" action = "/search">
        <input class="form-control me-2" type="search" placeholder="Search" aria-label="Search" name = "query" method = "GET">
        <button class="btn btn-outline-success" type="submit">Search</button> %>
        <%= search_form_for @q, url: "/home", method: :get do |f| %>
  <%= f.label :name_cont, 'Full Name' %>
  <%= f.search_field :name_cont %>
  <%= f.submit %>
<% end %>
<% end %>

      </form>
    </div>
  </div>
</nav>
    </body>
</html>
